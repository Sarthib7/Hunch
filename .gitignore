# Dependencies & build output (app/ also carries its own .gitignore)
node_modules/
.next/
out/
build/
*.tsbuildinfo

# Environment files & secrets — never commit
.env
.env.local
.env*.local

# Local Claude Code settings
.claude/settings.local.json

# Logs & OS cruft
*.log
.DS_Store
.vercel
